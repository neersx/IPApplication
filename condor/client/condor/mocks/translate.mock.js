angular.module('inprotech.mocks').factory('translateMock', function() {
    'use strict';

    var service = {
        instant: function() {}
    };

    test.spyOnAll(service);

    return service;
});
