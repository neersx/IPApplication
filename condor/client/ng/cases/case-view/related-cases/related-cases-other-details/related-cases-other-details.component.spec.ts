import { RelatedCasesOtherDetailsComponent } from './related-cases-other-details.component';

describe('RelatedCasesOtherDetailsComponent', () => {
  let component: RelatedCasesOtherDetailsComponent;

  beforeEach(() => {
    component = new RelatedCasesOtherDetailsComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
