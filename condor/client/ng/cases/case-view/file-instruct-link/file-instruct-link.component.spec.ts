import { FileInstructLinkComponent } from './file-instruct-link.component';

describe('FileInstructLinkComponent', () => {
  let component: FileInstructLinkComponent;

  beforeEach(() => {
    component = new FileInstructLinkComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
