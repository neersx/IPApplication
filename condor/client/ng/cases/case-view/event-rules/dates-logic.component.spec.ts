import { DatesLogicComponent } from './dates-logic.component';

describe('DatesLogicComponent', () => {
    let component: DatesLogicComponent;

    beforeEach(() => {
        component = new DatesLogicComponent();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});