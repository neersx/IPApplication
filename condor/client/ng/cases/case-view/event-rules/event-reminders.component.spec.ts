import { EventRemindersComponent } from './event-reminders.component';

describe('EventRemindersComponent', () => {

    let component: EventRemindersComponent;

    beforeEach(() => {
        component = new EventRemindersComponent();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});