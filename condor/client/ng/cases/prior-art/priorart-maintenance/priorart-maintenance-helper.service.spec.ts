import { PriorartMaintenanceHelper } from './priorart-maintenance-helper';

describe('PriorartMaintenanceHelper', () => {
  it('should create an instance', () => {
    const service = new PriorartMaintenanceHelper();
    expect(service).toBeTruthy();
  });
});
