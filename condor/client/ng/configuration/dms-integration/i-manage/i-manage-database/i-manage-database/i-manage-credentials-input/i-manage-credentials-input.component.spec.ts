import { IManageCredentialsInputComponent } from './i-manage-credentials-input.component';

describe('IManageCredentialsInputComponent', () => {
  let component: IManageCredentialsInputComponent;

  beforeEach(() => {
    component = new IManageCredentialsInputComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
