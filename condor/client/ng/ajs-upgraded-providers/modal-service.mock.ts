export class ModalServiceMock {
    open = jest.fn();
    isOpen = jest.fn();
    openModal = jest.fn();
    close = jest.fn();
    cancel = jest.fn();
    canOpen = jest.fn();
    show = jest.fn();
}