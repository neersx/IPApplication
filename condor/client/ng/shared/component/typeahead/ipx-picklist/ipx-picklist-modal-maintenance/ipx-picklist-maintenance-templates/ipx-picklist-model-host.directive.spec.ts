import { IpxPicklistModelHostDirective } from './ipx-picklist-model-host.directive';

describe('IpxTopicHostDirective', () => {
  it('should create an instance', () => {
    // tslint:disable-next-line: no-null-keyword
    const directive = new IpxPicklistModelHostDirective(null);
    expect(directive).toBeTruthy();
  });
});
